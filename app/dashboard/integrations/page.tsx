import Link from "next/link";
import IntegrationCenter from "@/components/IntegrationCenter";
import { getAdminContext } from "@/lib/admin-data";
export const dynamic = "force-dynamic";
export default async function IntegrationsPage() { const { supabase, shop } = await getAdminContext(); if (!shop) return <p>No shop configured.</p>; const { data } = await supabase.from("integration_connections").select("provider,category,status,account_label,last_tested_at,last_error").eq("shop_id", shop.id); return <><header className="admin-header"><div><p className="eyebrow">CONNECTION CENTER</p><h1>Plug in your business tools</h1><p>Securely connect payment, commerce, file-delivery and future supplier accounts from one place.</p></div><Link className="secondary-button" href="/dashboard/suppliers">Open Supplier Hub</Link></header><section className="integration-summary"><div><span className="connection-orb connected"/><div><strong>{(data || []).filter((item) => item.status === "connected").length} connected</strong><small>encrypted account connections</small></div></div><p>Supplier catalog management stays in Supplier Hub; credentials can be managed here or from the supplier card.</p></section><IntegrationCenter initialConnections={data || []}/></>; }
