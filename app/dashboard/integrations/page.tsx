import Link from "next/link";
import { getAdminContext } from "@/lib/admin-data";
export const dynamic="force-dynamic";
export default async function IntegrationsPage(){const {supabase,shop}=await getAdminContext();if(!shop)return <p>No shop configured.</p>;const {data:i}=await supabase.from('shop_integrations').select('checkout_provider,active,google_web_app_url').eq('shop_id',shop.id).maybeSingle();const connected=Boolean(i?.active),google=Boolean(i?.google_web_app_url);return <><header className="admin-header"><div><p className="eyebrow">CONNECTIONS</p><h1>Business integrations</h1><p>Payments, checkout, file delivery and accounting live here. Wholesale accounts now live in Supplier Hub.</p></div><Link className="secondary-button" href="/dashboard/suppliers">Open Supplier Hub</Link></header><section className="integration-summary"><div><span className="connection-orb connected"/><div><strong>{Number(connected)+Number(google)} connected</strong><small>business integrations</small></div></div><p>Supplier credentials and blank ordering are managed separately under Suppliers.</p></section><div className="integration-grid integration-grid-polished"><section className="admin-card integration-card"><div className="integration-logo">SQ</div><div className="card-heading"><div><h2>Squarespace Commerce</h2><p>Checkout provider</p></div><span className={connected?'status-pill connected':'status-pill'}>{connected?'Connected':'Setup needed'}</span></div><p>Handles checkout, taxes, shipping, discounts and payment receipts.</p></section><section className="admin-card integration-card"><div className="integration-logo google">GD</div><div className="card-heading"><div><h2>Google Drive + Sheets</h2><p>Order delivery</p></div><span className={google?'status-pill connected':'status-pill'}>{google?'Connected':'Setup needed'}</span></div><p>Receives paid-order artwork, previews and production information.</p></section><section className="admin-card integration-card muted-card"><div className="integration-logo stripe">ST</div><div className="card-heading"><div><h2>Stripe Connect</h2><p>Native payments</p></div><span className="status-pill">Roadmap</span></div><p>Merchant onboarding and direct payouts.</p></section></div></>}
