import "./advanced-admin.css";

export const metadata = {
  title: "Advanced Admin",
  robots: { index: false, follow: false }
};

export default function AdvancedAdminRootLayout({ children }: { children: React.ReactNode }) {
  return children;
}
